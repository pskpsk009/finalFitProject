const userProfile = require('./userProfile.json'); // Adjusted path to match the file structure

if (userProfile.weight === null || userProfile.height === null) {
  alert('Please set your weight and height in the user profile.');
}



// Example usage of logout function
// logout();
